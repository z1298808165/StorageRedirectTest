name: A17

on:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: A17-${{ github.ref }}
  cancel-in-progress: true

env:
  ANDROID_API_LEVEL: 37
  ANDROID_TARGET: google_apis
  ANDROID_ARCH: x86_64
  SRX_REPO: Kindness-Kismet/Storage-redirection-X-Public

jobs:
  test:
    name: Android 17 / API 37
    runs-on: ubuntu-latest
    timeout-minutes: 120

    steps:
      - name: 设置 Android SDK
        uses: android-actions/setup-android@v4

      - name: 检查 API 37 是否可用
        run: |
          set -euo pipefail
          sdkmanager --list > sdkmanager-list.txt
          platform="platforms;android-${ANDROID_API_LEVEL}"
          image="system-images;android-${ANDROID_API_LEVEL};${ANDROID_TARGET};${ANDROID_ARCH}"
          if grep -Fq "$platform" sdkmanager-list.txt && grep -Fq "$image" sdkmanager-list.txt; then
            echo "A17_AVAILABLE=true" >> "$GITHUB_ENV"
            exit 0
          fi
          echo "A17_AVAILABLE=false" >> "$GITHUB_ENV"
          echo "API ${ANDROID_API_LEVEL} is not available in the current Android SDK repository."
          echo "Missing: $platform or $image"

      - name: 检出代码
        if: env.A17_AVAILABLE == 'true'
        uses: actions/checkout@v6

      - name: 设置 JDK 21
        if: env.A17_AVAILABLE == 'true'
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '21'

      - name: 设置 Gradle
        if: env.A17_AVAILABLE == 'true'
        uses: gradle/actions/setup-gradle@v6

      - name: 启用 KVM 权限
        if: env.A17_AVAILABLE == 'true'
        run: |
          set -euo pipefail
          if [ -e /dev/kvm ]; then
            sudo sh -c 'echo KERNEL==\"kvm\", GROUP=\"kvm\", MODE=\"0666\", OPTIONS+=\"static_node=kvm\" > /etc/udev/rules.d/99-kvm4all.rules'
            sudo udevadm control --reload-rules
            sudo udevadm trigger --name-match=kvm
          fi

      - name: 准备脚本
        if: env.A17_AVAILABLE == 'true'
        run: chmod +x ./gradlew .github/scripts/*.sh

      - name: 下载最新版 Storage Redirect X 模块
        if: env.A17_AVAILABLE == 'true'
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          mkdir -p core
          TAG="$(gh release view --repo "$SRX_REPO" --json tagName -q .tagName)"
          gh release download "$TAG" --repo "$SRX_REPO" --pattern '*x86_64.zip' --dir core --clobber
          test -n "$(find core -maxdepth 1 -name '*x86_64.zip' -print -quit)"

      - name: 构建 APK 并运行本地测试
        if: env.A17_AVAILABLE == 'true'
        run: ./gradlew --no-daemon --stacktrace :app:testDebugUnitTest :media-file-api:testDebugUnitTest :app:assembleDebug :app:assembleDebugAndroidTest :media-file-api:assembleDebugAndroidTest

      - name: 运行模拟器测试
        if: env.A17_AVAILABLE == 'true'
        uses: reactivecircus/android-emulator-runner@v2.37.0
        with:
          api-level: ${{ env.ANDROID_API_LEVEL }}
          target: ${{ env.ANDROID_TARGET }}
          arch: ${{ env.ANDROID_ARCH }}
          profile: pixel_7
          disk-size: 4096M
          heap-size: 1024M
          disable-animations: true
          emulator-boot-timeout: 1500
          script: |
            set -eu
            adb wait-for-device
            adb shell 'while [ "$(getprop sys.boot_completed)" != "1" ]; do sleep 2; done'
            bash .github/scripts/install-storage-redirect-module.sh
            adb install -r app/build/outputs/apk/debug/app-debug.apk
            adb shell appops set me.fakerqu.test.storageredirect MANAGE_EXTERNAL_STORAGE allow || true
            bash .github/scripts/run-storage-redirect-scenarios.sh

      - name: 上传测试产物
        if: always() && env.A17_AVAILABLE == 'true'
        uses: actions/upload-artifact@v7
        with:
          name: A17-artifacts
          if-no-files-found: ignore
          retention-days: 14
          path: |
            core/*.zip
            scenario-*-result.txt
            media-health.txt
            app/build/outputs/apk/**/*.apk
            app/build/reports/
            app/build/outputs/androidTest-results/
            media-file-api/build/reports/
            media-file-api/build/outputs/androidTest-results/
